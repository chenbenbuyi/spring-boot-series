BMMN2.0使用一些符号来明确业务流程设计。其基本符号包括：

事件(Event)
开始事件(start event)、中间事件(intermediate event)、结束事件(end event)

活动(Activity)，工作或任务的通用术语，一个活动可以是一个任务，还可以是一个当前流程的子处理流程。
常见的活动有：用户任务（User Task）,服务任务（Service Task）,子流程（Sub Process）

网关(GateWay)
排他网关、并行网关、包容网关、综合网关、事件网关

流向

一些关键点
1、流程图画完之后，点击空白区域设置Id值，即指定流程定义的Key,即流程定义的标识（如果有两个相同标识的Key,部署流程直接抛异常），顺便为流程起好名称
2、如何和业务关联
 先定义、部署流程，这是业务无关的
 启动流程实例，将业务数据和流程实例相关联


